# Vercel 授权给我(不用给密码)

> 目的:让我能自己看构建日志、改环境变量、验证部署 —— 以后你不用截图给我

---

## 方法:设备码登录(谷歌官方流程,不涉及密码)

我会在你服务器上跑一条命令,它会给你一个**网址 + 8 位代码**:

```
你做的:
① 打开那个网址(vercel.com/oauth/device)
② 输入代码
③ 用你的 Vercel 账号(dillopaula006@gmail.com)点授权
④ 完成 ✅

我不需要知道你的密码 —— 谷歌/ Vercel 官方机制
```

**授权后我能做**:
```
✓ 看构建日志(以后构建失败我直接看原因,不用你截图)
✓ 看/改环境变量(比如 AdSense ID、API keys)
✓ 手动触发重新部署
✓ 看部署状态和访问量
✓ 解析域名(以后绑 Cloudflare / 域名)
```

**我不能做**:
```
✗ 不知道你的密码(device flow 不传密码)
✗ 不会动你的付款/账单(需要额外授权)
✗ 随时可以撤销:Vercel → Settings → Tokens/Apps → 撤销
```

---

## 如果你想授权

**跟我说一句"授权",我就把网址和代码发你。**

流程 1-2 分钟。

---

## 如果你不想授权(也没关系)

```
我还能这样做:
✓ 本地跑构建拿错误(现在正在跑)
✓ 靠 curl 验证线上是否 200
✗ 但看不到 Vercel 的原始日志(只能靠猜 + 本地复现)
```

**取舍**:授权后排查效率高很多(构建错误直接看),
但不授权也能干活,只是慢一点。

---

## 顺便:那个邮箱

```
dillopaula006@gmail.com
→ 看起来是你早年注册各种服务的账号

建议(不是必须):
  · 以后对外申请联盟/服务,用 info@nocodecsv.com
    (专业邮箱通过率高,昨天说过)
  · 但 Vercel/GitHub 这些基础设施账号继续用旧的没关系
```
